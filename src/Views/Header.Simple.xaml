<Grid xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      x:Name="HeaderRoot">

    <!-- All colors come from theme resources -->
    <Border CornerRadius="8"
            Padding="12"
            SnapsToDevicePixels="True"
            Background="{DynamicResource BrushHeaderBG}"
            BorderBrush="{DynamicResource BrushHeaderBorder}"
            BorderThickness="0,0,0,1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- Left: logo + title/subtitle -->
            <StackPanel Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Margin="4,0,8,0">

                <!-- Logo with subtle hover scale -->
                <Image x:Name="HeaderLogo"
                       Width="88" Height="88"
                       Stretch="Uniform"
                       Opacity="0.95"
                       Cursor="Hand"
                       RenderTransformOrigin="0.5,0.5"
                       ToolTip="Application logo">
                    <Image.RenderTransform>
                        <ScaleTransform ScaleX="1" ScaleY="1"/>
                    </Image.RenderTransform>
                    <Image.Style>
                        <Style TargetType="Image">
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                                 To="1.12" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                                 To="1.12" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 To="1.0" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleX"
                                                                 To="1.0" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="RenderTransform.ScaleY"
                                                                 To="1.0" Duration="0:0:0.15"/>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 To="0.95" Duration="0:0:0.15"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Image.Style>
                </Image>

                <!-- Texts -->
                <StackPanel Orientation="Vertical" VerticalAlignment="Center" Margin="16,0,0,0">
                    <TextBlock x:Name="HeaderTitle"
                               Text="BLUARCH"
                               FontSize="48"
                               FontWeight="Bold"
                               Foreground="{DynamicResource BrushHeaderFG}"/>
                    <TextBlock x:Name="HeaderSubtitle"
                               Text="PowerShell Module Installer"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Margin="0,4,0,0"
                               Foreground="{DynamicResource BrushHeaderSubFG}"/>
                </StackPanel>
            </StackPanel>

            <!-- Right: action icons go here (your Add-WpfHeader adds Image children) -->
            <StackPanel x:Name="HeaderActions"
                        Grid.Column="1"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right"
                        Margin="8,0,4,0">
                <!-- Tip: if you later decide to add Buttons instead of raw Images,
                     you can template them here and use AccentBrush/hover brushes -->
            </StackPanel>
        </Grid>
    </Border>
</Grid>
